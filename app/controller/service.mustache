apiVersion: v1
kind: Service
metadata:
  name: {{{ dummysite_name }}}-service
spec:
  type: ClusterIP
  selector:
    app: {{{ dummysite_name }}}
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
